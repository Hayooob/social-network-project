# Base URL of the Go API.
#
# Leave empty in production: the app is served behind nginx, which proxies
# /api, /ws and /uploads to the backend, so relative URLs are correct.
# In development, Vite proxies /api to localhost:8080 (see vite.config.js),
# so this is only needed when the API runs somewhere else.
VITE_API_URL=
